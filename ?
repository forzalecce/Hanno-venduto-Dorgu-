<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>Hanno Venduto Dorgu?</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: repeating-linear-gradient(
                to right,
                yellow 0px, yellow 50px,
                red 50px, red 100px
            );
            font-family: Arial, sans-serif;
        }
        #status {
            color: black;
            font-weight: bold;
            font-size: 50px;
        }
        #timestamp {
            position: fixed;
            bottom: 10px;
            right: 10px;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div id="status">Non ancora</div>
    <div id="timestamp"></div>

    <script>
        function updateTimestamp() {
            const timestampElement = document.getElementById('timestamp');
            timestampElement.textContent = new Date().toLocaleString('it-IT');
        }

        // Initial timestamp
        updateTimestamp();

        // Update timestamp every 5 minutes
        setInterval(updateTimestamp, 5 * 60 * 1000);
    </script>
</body>
</html>
